#lang racket

(provide ask)

; CONSTRUCTOR
; ---- POR DEFINIR -----

; MODIFICADOR
;descripción: Función que permite realizar una pregunta y agregarla al usuario dentro del stack
;dom: stack
;rec: stack
(define (ask stack) stack)
